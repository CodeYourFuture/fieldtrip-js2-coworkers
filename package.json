{
  "private": true,
  "name": "cyf-learning-lab",
  "version": "1.0.0",
  "author": "Daniel Grant",
  "license": "ISC",
  "homepage": "https://github.com/codeyourfuture/cyf-learning-lab",
  "workspaces": [
    "apps/*",
    "courses"
  ],
  "scripts": {
    "build": "lerna run build",
    "dev": "lerna run dev --parallel",
    "dev:packages": "lerna run dev --parallel --scope @packages/* --scope @courses/*",
    "start": "yarn workspace server start",
    "test": "yarn build -- --noEmit && jest"
  },
  "devDependencies": {
    "@types/jest": "^27.4.0",
    "babel-preset-react-app": "^10.0.1",
    "jest": "^27.4.7",
    "lerna": "^4.0.0",
    "ts-jest": "^27.1.3"
  },
  "resolutions": {
    "mini-css-extract-plugin": "2.4.5"
  }
}
